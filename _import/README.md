# Media import

Upload all files named `fgfe-ready.zip.part-00` ... `fgfe-ready.zip.part-07` into this `_import/` directory on `main`.
The GitHub Action will rebuild the archive, import all binary media into the repository, and remove `_import/` automatically.
